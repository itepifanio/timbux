Let crossProduct
    float b = 1.2 + 3;
    int a = -2 + 4;
    int i = a * b;
    if(i >= a * b && b > a)
        int c = 20;
        int j = a * c;
    endif

    if(i >= a * b || b < a)
        int c = 20;
        int j = a * c;
    endif

    b = 10.1;
Ghbc
